#include<iostream>
using namespace std;
int lar(int p[],n)
{
    long i,l=p[1],
    for(i=1;i<=n-i-1;i++)
    {
        if(l<p[i])
            l=p[i];
    }
    return l;
}
int main()
{
    int t,r;
    cin>>t;
    while(t--)
    {
        long n,m,i,s=0,k,sd=0,e[n],c=0,j,lar,d;
        cin>>n>>m;
        long p[n],
        for(i=1;i<=n;i++)
            cin>>p[i];
        for(i=1;i<=m;i++)
            cin>>l[i]>>r[i];
        for(i=2;i<=n;i++)
        {
            for(j=l[i];j<=r[i])
            {
                    if(lar<p[j])
                    {lar=p[j];d=j}
            }
            e[c++]=d;
        }
        for(i=1;i<=c;i++)
        {

        }
    cout<<sd<<endl;
    }
    return 0;
}
